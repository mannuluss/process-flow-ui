{
  "name": "@process-flow/common",
  "version": "0.0.1",
  "type": "module",
  "main": "dist/index.cjs",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "require": "./dist/index.cjs",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "pnpm run build:tsup",
    "build:tsup": "tsup src/index.ts --format cjs,esm --dts --out-dir dist --clean",
    "build:tsc": "tsc -p tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "tsup": "^6.7.0",
    "typescript": "^5.3.3"
  }
}
