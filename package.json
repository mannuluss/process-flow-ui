{
  "name": "process-flow-monorepo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint",
    "build:packages": "pnpm --filter @process-flow/common build",
    "dev:frontend": "pnpm --filter @process-flow/frontend dev",
    "dev:backend": "pnpm --filter @process-flow/backend start"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
